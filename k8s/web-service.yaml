apiVersion: v1
kind: Service
metadata:
  name: web-service
  labels:
    app: web
spec:
  ports:
  - name: http
    port: 80
    targetPort: 5000
    nodePort: 30000
  selector:
    app: web
  type: NodePort
